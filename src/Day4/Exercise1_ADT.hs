module Day4.Exercise1_ADT where

{- Create an ADT for different entities:
   Customer should contain the following fields: id, first name, last name.
   Credit should contain the following fields: id, borrower id, loan amount, monthly payment.
   Card type with two possible values: Debit and Credit.
   Card should contain the following fields: card number, card holder id, card type, security code. -}
