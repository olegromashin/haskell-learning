module Day8.Exercise1_MonadTransformers where

{- Add configurator library to dependencies, create and parse config with 3 custom settings.
   Run computration in ReaderT monad from IO with config as environment.
   Print all settings from config inside this ReaderT computation. -}
